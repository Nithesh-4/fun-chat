{
  "name": "fun-chat-root",
  "version": "1.0.0",
  "description": "Root package runner for fuN ChaT monolith",
  "scripts": {
    "install-frontend": "npm install --prefix frontend --legacy-peer-deps",
    "install-backend": "npm install --prefix backend",
    "build-frontend": "npm run build --prefix frontend",
    "build": "npm run install-frontend && npm run build-frontend",
    "start": "npm run install-backend && npm start --prefix backend"
  }
}
